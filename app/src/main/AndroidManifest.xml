<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.suyue.myapplication">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KeyActivity"
            android:parentActivityName=".MainActivity" />
        //parentActivityName is only used in API level 16 or higher
        <activity
            android:name=".BoxActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TouchActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ListActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DialogActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".IntentActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".FIntentActivity"></activity>
    </application>

</manifest>